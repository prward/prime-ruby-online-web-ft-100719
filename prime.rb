# Add  code here!
require 'benchmark'
require 'bigdecimal/math'

def big_math(max)
  primes = (2..max).to_a
  counter = 0
  primes.each do |p|
    next unless p













def prime?(int)
  
end
  
  